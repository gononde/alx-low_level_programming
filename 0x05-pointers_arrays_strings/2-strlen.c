#include "main.h"
#include <stdio.h>
/**
 * _strlen - returns the length of a string
 * @s: string parameter
 * Return: lenght of string
 */
int _strlen(char *s)
{
	int index;

	for (index = 0; index != '\0'; index++)
	return (index);
}
